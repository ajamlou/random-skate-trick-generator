import React from "react";
import HomePage from "../../components/home";

const Home = (props) => {
  return (
    <section className="homepage">
      <HomePage />
    </section>
  );
};

export default Home;
