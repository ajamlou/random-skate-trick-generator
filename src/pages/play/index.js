import React from "react";
import PlayGame from "../../components/playGame";

const Play = (props) => {
  return (
    <section className="playPage">
      <PlayGame />
    </section>
  );
};

export default Play;
